import {AbstractControl,ValidationErrors} from  '@angular/forms'


export class LoginValidation{
static userNameValidation(control:AbortController):ValidationErrors | null{
   // if((control.value as string) )
    return null;
}
}